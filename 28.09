import simfin as sf
import pandas as pd

sf.set_api_key('759cbb1b-5114-4a25-a420-bd2835c231d4')
sf.set_data_dir('~/simfin_data/')

income_df = sf.load_income(variant = 'quarterly', market = 'us').reset_index() #reset index allows for columns to be numbered. Req bc Pandas dataframe is mult-indexed.
income_df

cashflow_df = sf.load_cashflow(variant = 'quarterly', market = 'us').reset_index()
cashflow_df

balance_df = sf.load_balance(variant = 'quarterly', market = 'us').reset_index()
balance_df

income_df.info()

balance_df.info()

cashflow_df.info()
